extends layout

block content

  h1= title
  - var image_names = ["Convectionoven.jpg","Microwaveoven.jpg","Regular Oven.jpg","Smart Oven.jpg","toasteroven.jpg"]
  - for(let i=0; i<3;i++){
    -var num = image_names[Math.floor(Math.random() * 5)]
    
    img(src= "/images/"+num, alt= num, style={width: "300",height: "300"})
  -}
  